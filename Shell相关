glob模块
可以使用Unix shell风格的通配符匹配符合特定格式的文件和文件夹
glob.glob(pathname, recursive=False)  
返回匹配列表
glob.glob("s%/*.sh" %task)
#默认不匹配以点符号（.）开始的文件，需要特殊处理
>>> glob.glob('.*.txt')
['.test.txt']

shlex模块
为shell语法编写词法分析器
shlex.split("grep -c success s%" %cmd)
#使用类shell语法拆分字符串
